# Ranges rather than exact pins so Streamlit Cloud rebuilds stay reproducible
# without going stale. Once the app is running, freeze the exact versions:
#   pip freeze | grep -iE "streamlit|requests|pandas|openpyxl|yfinance"

streamlit>=1.40,<2   # width/use_container_width is feature-detected, so old and new both work
requests>=2.31,<3
pandas>=2.1,<3
openpyxl>=3.1,<4

# Optional. The app imports it defensively and disables the "live market cap"
# button if it is missing or broken, so you can delete this line if yfinance
# starts failing again — everything else keeps working on SEC data alone.
yfinance>=0.2.40,<0.3
